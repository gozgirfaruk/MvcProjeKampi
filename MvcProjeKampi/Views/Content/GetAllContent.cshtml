﻿@using EntityLayer.Concrete
@model List<Content>
@{
    ViewBag.Title = "GetAllContent";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

@using(Html.BeginForm("GetAllContent","Content",FormMethod.Post))
            {
    <p>Aranacak Kelimeyi Gir : @Html.TextBox("p")
        <input type="submit" value="Ara" class="btn btn-outline-secondary"/>
        </p>
    
            }

@foreach (var item in Model)
{
    <p>@item.ContentValue</p>
}